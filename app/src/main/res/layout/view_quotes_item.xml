<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    
    <data>
        <variable
            name="quotes"
            type="com.web0zz.wallquotes.domain.model.Quotes" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <TextView
            android:id="@+id/quote_textView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{quotes.body}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            tools:text="&quot;Design isn't finished until somebody is using it&quot;" />

        <TextView
            android:id="@+id/writer_textView"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{quotes.authorName}"
            app:layout_constraintEnd_toEndOf="@+id/quote_textView"
            app:layout_constraintStart_toStartOf="@+id/quote_textView"
            app:layout_constraintTop_toBottomOf="@+id/quote_textView"
            tools:text="Jeff Lindsay" />

        <ImageButton
            android:id="@+id/like_imageButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/writer_textView" />

        <ImageButton
            android:id="@+id/extra_imageButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintEnd_toEndOf="@+id/like_imageButton"
            app:layout_constraintStart_toStartOf="@+id/like_imageButton"
            app:layout_constraintTop_toBottomOf="@+id/like_imageButton" />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>